.venv/
__pycache__/
*.py[cod]
.env
.env.*
!.env.example
*.gpkg
*.osm.pbf
*.parquet
.ipynb_checkpoints/
.pytest_cache/
*.egg-info/

# Pipeline data sits in data/ so the code is beside it, but re-downloads or
# recomputes. Everything else under data/ is tracked, including data/llm/,
# which holds answers no rerun can reproduce.
data/raw/
data/derived/
data/artifacts/
tests/fixture/derived/place_context.json
tests/fixture/derived/street_*.json

# Local-only: working notes and Claude Code project config. Untracked
# deliberately; their history lives outside this repo. Do not re-add.
CLAUDE.md
.claude/
