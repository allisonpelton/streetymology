# Exact versions this project was last verified against.
# The transitive closure of requirements.txt, resolved against the versions
# installed in .venv rather than against the index, so it records what the
# tests actually passed on. Regenerate by re-running that resolution; there is
# no pin script (an earlier header named scripts/pin_requirements.py, which the
# repo has never contained).
# python 3.11.2, generated 2026-09-17

certifi==2026.7.22
charset-normalizer==3.5.1
idna==3.19
iniconfig==2.3.0
numpy==2.4.6
packaging==26.3
pluggy==1.6.0
Pygments==2.21.0
pyproj==3.7.2
pytest==9.1.1
python-dotenv==1.2.3
requests==2.34.2
shapely==2.1.2
urllib3==2.7.0
